# 种子数据（CSV）

由 **数据席**（串行模式）或各 **模块席**（并行模式）维护，**脱敏后**提交 Git。手册见 `docs/AI开发手册-串行主编模式.md`。

| 文件 | 说明 | 状态 |
|------|------|------|
| `brands.csv` | 品牌主数据 | 已有表头+5 品牌 |
| `contacts.csv` | 关键人 | 待建 |
| `visits-demo.csv` | 示例拜访 | M3 前需 10 条 |
| `intel-demo.csv` | 示例情报 | M4 前需 15 条 |

**主编席 / 集成主编** 在 M0/M1（或 I0）验收通过后，再让 AI 实现 `npm run seed:csv` 灌库。
