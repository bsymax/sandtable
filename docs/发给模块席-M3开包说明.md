# 发给模块席 · M3 开包说明

> Max 发群 · 配合 `*-m3-0622-baseline.zip`

---

## 1. 收到了什么

| 文件 | 给谁 |
|------|------|
| `jiaxuan-m3-0622-baseline.zip` | 佳璇 |
| `peixiao-m3-0622-baseline.zip` | 培翛 |
| `kaikai-m3-0622-baseline.zip` | 开开 |
| `sandtable-m3-reference-readonly.zip` | 全员只读对照（**勿改**） |

包内已含 **`docs/M3并行开发手册-正式版.md`**，日常以该手册为准；协作铁律与 M2 相同（baseline → zip → Max 合并，**不 clone git**）。

---

## 2. M3 和 M2 有什么不同

| 项 | M2 | M3 |
|----|-----|-----|
| LLM | 无，仅规则版槽位 | **五路 LLM**（关则回退 M2 规则版） |
| 登录 | 无 | **必须**（用户 ↔ 负责品牌） |
| 数仓 | seed 为主 | **v1 日更同步** → `brand_metrics` |
| 工程 | 功能加深 | M3 收官 **不含** 50 并发压测（→ **M4～M6**，见正式手册 §0.5） |
| 排期 | 0616～618 三天 | **单窗口 ~8 工作日**（**不按 W1/W2 日历切**） |

---

## 3. 三人现在做什么

1. 解压 **自己的** `*-m3-0622-baseline.zip`，**旧 M2 包整包停用**
2. 读包内 `README-BASELINE.md` + `docs/M3并行开发手册-正式版.md` §3.2 你的任务行
3. **Max 阻塞项未就绪前**：先做 UI/降级/mock；**LLM 失败不算 bug**，显示规则版即合格
4. 自测通过后打 **`xxx-m3-0622.zip`**（无 `-baseline`）发 Max
5. 外网验收为准：http://117.72.211.51/ · **Cmd+Shift+R**

---

## 4. 依赖 Max 的三件事（群里看公告，不必记 W1/W2）

| 顺序 | Max 交付 | 没到之前你可以 |
|------|----------|----------------|
| ① | 登录 + Session + 品牌权限 API | 用 mock 用户；勿改主工程 |
| ② | LLM 中台（开关/超时/降级） | Tab/提醒用 M2 规则版 UI 占位 |
| ③ | 数仓 v1 同步 + 合并上云 | 指标仍可读 seed；标注待同步 |

Max 每交付一项会在群里 `@全员`，再对接真 API。

---

## 5. 报进度模板

```text
【档案/拜访/情报】【M3 开发】D-J-M3-1 完成
【拜访】【M3 开发】卡住 — 等 Max LLM 中台 + 截图
【档案】【M3 补丁】jiaxuan-m3-patch-MMDD.zip 已发 Max
```

---

## 6. 常见问题

**Q：还要分 W1、W2 吗？**  
A：**不用。** M3 只有一个 **~8 工作日窗口**；上面表格是 **依赖顺序**，不是「第 1 周 / 第 2 周」。

**Q：M2 补丁还能交吗？**  
A：M2 已收官；线上 hotfix 仍可走 `*-m3-patch-*.zip`，由 Max 甄别。

**Q：本地比外网旧？**  
A：正常。验收以外网 tag `prod-web-2026-06-18` 为准；本 baseline 已从该基线同步。

---

*Max · M3 baseline m3-0622 · 细节见 `docs/M3并行开发手册-正式版.md`*
