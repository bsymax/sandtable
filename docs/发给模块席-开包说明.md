# 发给模块席 · M1 基准包开包说明

> Max 可直接复制下面段落发到群里，附件为 `deliverables/` 下 4 个 zip。

---

各位好，M1 联调已通过，发你们 **M1 基准包**，后面 M2/M3 请在这上面继续，避免和主工程分叉。

## 你会收到 4 个文件

| 文件 | 给谁 | 用途 |
|------|------|------|
| `jiaxuan-m1-0612-baseline.zip` | 佳璇 | 品牌档案模块基准 |
| `kaikai-m1-0612-baseline.zip` | 开开 | 情报模块基准 |
| `peixiao-m1-0612-baseline.zip` | 培翛 | 拜访模块基准 |
| `sandtable-m1-reference-readonly.zip` | 全员 | **只读**对照整站效果，不要在这里改代码 |

另附文档：`docs/M1联调基准说明.md`（详细变更清单，尤其开开必读「周报改 brand_metrics」）。

## 三人统一规则

1. **日常开发在自己的 baseline 包里改**，改完 zip 发我合并（不要 clone 主工程 git）  
2. 包内 **`synced-from-main/`** 是从我电脑主工程拷的最新代码，**M2 以它为准**  
3. 公共表（`brands`、`visits` 等）要加字段 → 群里「字段申请」，我批准后再动  
4. 看完整联调效果：外网 **http://117.72.211.51/**（**06-14～15 外网验收**，见主手册「📅 06-15 M1 收官计划」）

## 各模块一句话

- **佳璇**：`frontend/profile.html` 已含 Tab3/4 真 API；你 M2 主要盯 Tab1/2 + 完整度 + 自己的表  
- **开开**：⚠️ **`intel_weekly_reports` 已废弃**，周报进 `brand_metrics`；务必读基准说明 §3.3  
- **培翛**：S2 + 三个 Tab 独立品牌筛选已在 baseline；M2 继续拜访链路即可  

## 自测

每人打开自己包里的 **README.md**，按「快速启动」跑通后再开 M2。

有问题模块内先 AI 排查，合并/跨模块/上云找 Max。
