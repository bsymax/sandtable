# 品牌沙盘 M1 · 并行开发手册（正式版）

> V2.0 | 2026-06-10
> 本手册取代 `AI开发手册-并行集成模式.md` 中的技术栈假设（原计划 fork GANT 的 Next.js + Prisma + PG）。
> **拜访模块（培翛）已用 FastAPI + MySQL + 原生 HTML 交付并跑通，全队统一改用此栈。**
> Demo 与 UI 基准：`sandtableM1.html`（打开即可看到每个模块第一期要做成什么样）。

---

## 〇、一页速览

| 事项 | 约定 |
|------|------|
| 团队 | **Max**＝集成主编（合并/仲裁/部署）；**佳璇**＝品牌席；**培翛**＝拜访席；**开开**＝情报席 |
| 技术栈 | Python 3.10+ / FastAPI / SQLAlchemy / MySQL 8 / 原生 HTML+CSS+JS（零框架） |
| UI 基准 | `sandtableM1.html`——配色、卡片、Tab、标签样式全部照抄它 |
| 范例交付包 | `peixiao-m1-0610/`——所有人的交付包都长这样（backend + frontend + database + README） |
| 协作方式 | **交付包合并制**：模块席各自在本机独立开发→打包发 Max→Max 用 AI 合并进主工程 |
| 数据库 | 开发期各自本机一个 `brand_sandtable` 库；合并后以主工程库为准 |
| 端口 | 开发期：拜访 8000 / 品牌 8001 / 情报 8002；合并后主工程统一 8000 |
| 阶段 | S0(✅06-10) → S1(06-12) → S2a核心(★06-17) → 618+端午休 → S2b补充(06-22) → S4合并(06-24) → S5工作台(06-26) → S6上云(06-30)；周末一律不排 |
| 沟通 | 群里报进度用固定格式：`【模块】【阶段】完成/卡住 + 截图` |

---

## ✅ 项目 Check 清单（Max 维护，每过一个阶段更新）

> 项目开始时间：**2026-06-10 12:00** ｜ M1 截止：**2026-06-30**（2026-06-10 晚第二次调整）
> ⚠️ 排期硬约束：**周末（06-13/14、06-27/28）不排任务**；**06-18 是 618 大促**，白天只做缓冲、晚上不排任何事、且可能提前放端午假；**端午假期 06-19～21**。
> ★ **06-17（三）晚 = S2 核心交付硬时点**（全员在岗的最后一个完整工作日）。

| 阶段 | 负责人 | 核心验收标准（详见 §五） | 计划完成 | 实际验收时间 | 状态 |
|------|--------|--------------------------|----------|--------------|:----:|
| **S0 环境统一** | 全员 | ① `python3 --version` ≥3.10 ② `127.0.0.1:8000/docs` 打开 ③ 前端 5 品牌+种子数据 ④ 新拜访刷新后仍在（写库成功） | 06-13 | **2026-06-10**（Max 午后复现培翛包；佳璇/开开 当晚通过） | ✅ |
| **S1 数据层** | 佳璇/开开 | ① schema.sql 一次过且可重复执行 ② /docs 里 GET 返种子数据 ③ snake_case + created_at/updated_at ④ 不动培翛 7 张表结构 | **06-12（五）晚** | | ☐ |
| **S2a 核心功能** ★硬时点 | 佳璇/开开 | 佳璇：5 品牌切换真数据、关键人可编辑入库、6 指标卡来自库、样式对齐 demo；开开：录入 P0 即时标红、筛选正确、刷新仍在 | **06-17（三）晚**<br>中期检查 06-16 晚 | | ☐ |
| **S2b 完善补充包**（原 S3 必做项） | 佳璇/开开/培翛 | 佳璇：图表随品牌刷新无报错；开开：搜索+周报区块、brand_id 对齐；培翛：承诺状态可改且 API 同步 | **06-22（一）晚**<br>（端午后回来第一天） | | ☐ |
| **S4 滚动合并** | Max | ① 一条命令启动 server/，/docs 三组 API 齐 ② 合并版 schema 一次过 ③ 统一壳四导航真数据 ④ 跨模块跳转通 ⑤ 完整业务流走通 ⑥ 三人实操 10 分钟无报错 | **06-24（三）**<br>（06-18 白天可提前开合 S2a） | 第一步（拜访基底入主工程）**2026-06-10 提前完成** | 🔶 |
| **S5 工作台聚合** | Max | ① 工作台数字与各模块一致 ② 待办勾完成双向同步 | **06-26（五）** | | ☐ |
| **S6 上云+内部试点** | Max | ① 外网可访问 ② 数据不丢 ③ 50 并发可用 ④ 团队 4 人外网跑通全流程 | **06-30（二）**（06-29~30 执行） | | ☐ |

**排期说明（第二次调整要点）**
1. **S1 压到 2 个工作日**（06-11~12）：任务本身小（建表+GET 接口），但周末不排，必须周五晚交
2. **S2 拆两包**：核心包 06-17 晚硬交付（618 与端午前全员在岗的最后完整节点）；完善包（原 S3 项）端午后 06-22 晚补交，**不阻塞 Max 合并**
3. **06-18（618）**：白天 Max 可提前开始合并 S2a 核心包；模块席这天不排任务（大促 + 可能提前走）
4. **S4~S6 全部排在工作日**：合并 06-22~24、工作台 06-25~26、周末休、上云 06-29~30
5. 移出 M1 不变：档案完整度计算、情报 AI 摘要、拜访纪要 AI 抽待办（M2 再做）；外部试点反馈收集放 7 月初

---

## 一、为什么这样定（背景 3 分钟）

1. **四人都不写代码**，全部靠 AI（Cursor / Claude 等）。所以本手册的每一步都给出「可以直接粘贴给 AI 的话术」。
2. 培翛的拜访模块已经完成：FastAPI 后端 + MySQL 建表脚本 + 对接 API 的前端页面，结构清晰、能独立跑通。**与其让她推倒重来迁就别的栈，不如全队向她看齐**——这是已被验证可行的路线。
3. 不用 git 分支协作（模块席不懂 merge，出冲突修不动）。改用**交付包合并制**：每人交一个像 `peixiao-m1-0610` 一样的文件夹，Max 负责合并。git 只由 Max 一人用于主工程版本管理。

---

## 二、角色与职责

### Max（集成主编）——唯一合并权

- 维护主工程 `sandtable/server/` + `sandtable/web/`（S4 起出现）
- 验收每个阶段的交付包；合并；合并失败时用 AI 修到能跑
- 仲裁所有**公共资源**：`brands` 品牌主数据表、API 路径、端口、UI 公共样式
- 唯一有权改主工程数据库结构的人（模块席提「字段申请」）
- 管理 git 提交与最终上云部署

### 佳璇（品牌席）

- 范围：品牌档案模块 = `sandtableM1.html` 中「品牌档案」四个 Tab（经营看板 / 品牌简介 / 最新情报 / 历史互动）里**前两个 Tab** 的真实化（后两个 Tab 依赖情报和拜访数据，在 S4 由 Max 聚合）
- 交付物：`jiaxuan-m1-XXXX/` 交付包（backend + frontend + database + README）
- 注意：`brands`、`brand_contacts` 两张表培翛已先建（拜访模块要用），**佳璇基于培翛的表结构扩展，不要另起炉灶**（见 §六 公共表规则）

### 培翛（拜访席）——已交付 ✅

- `peixiao-m1-0610/` 已完成 S1+S2+S3 的拜访部分，是全队的**标准范例**
- 后续职责：配合 Max 合并联调；S3 收尾（见 §五.S3）；给佳璇、开开讲解她的开发经验（一次 30 分钟分享）

### 开开（情报席）

- 范围：品牌情报流 = `sandtableM1.html` 中「品牌情报流」页：录入表单、P0/P1/P2 分级、Feed 列表、品牌/级别筛选、搜索
- 交付物：`kaikai-m1-XXXX/` 交付包

---

## 三、每个人的开发环境（S0 要全员完成）

### 3.1 需要安装的东西（一次性）

| 软件 | 用途 | 装法（粘贴给 AI 让它带你装） |
|------|------|------|
| Python 3.10+ | 跑后端 | 「帮我在 Mac/Windows 上安装 Python 3.12，并验证 python3 --version」 |
| MySQL 8 | 数据库 | 推荐 Docker 方式（下方命令）；或 MySQL 官网安装包 |
| Cursor | AI 写代码 | 官网下载 |
| 浏览器 | 看页面 | Chrome / Edge |

**Docker 跑 MySQL（推荐，全队统一这条命令）：**

```bash
docker run -d --name sandtable-mysql \
  -e MYSQL_ROOT_PASSWORD='Sandtable@2026' \
  -p 3306:3306 \
  -v sandtable-mysql-data:/var/lib/mysql \
  mysql:8 \
  --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
```

> 统一 root 密码 `Sandtable@2026`，写进各自 `.env`，交付包就能互相直接跑（`.env` 不要发到群里截图）。

**Docker 装不上的备选：免安装版 MySQL（Max 本机实际采用此方式）**

```bash
mkdir -p ~/mysql-sandtable && cd ~/mysql-sandtable
curl -L -o mysql.tar.gz "https://cdn.mysql.com/Downloads/MySQL-8.0/mysql-8.0.46-macos15-arm64.tar.gz"
tar xzf mysql.tar.gz && ln -s mysql-8.0.46-macos15-arm64 mysql
xattr -r -d com.apple.quarantine mysql-8.0.46-macos15-arm64
mkdir data
./mysql/bin/mysqld --initialize-insecure --datadir=$HOME/mysql-sandtable/data --basedir=$HOME/mysql-sandtable/mysql
./mysql/bin/mysqld --datadir=$HOME/mysql-sandtable/data --basedir=$HOME/mysql-sandtable/mysql --port=3306 --bind-address=127.0.0.1 &
./mysql/bin/mysql -uroot --protocol=tcp -e "ALTER USER 'root'@'localhost' IDENTIFIED BY '你的密码';"
```

> ⚠️ 导入建表脚本时必须带字符集参数，否则中文变乱码：
> `mysql -uroot -p --default-character-set=utf8mb4 < database/schema.sql`

### 3.2 验证环境 OK 的标准动作

把培翛的范例包跑起来（这同时是 S0 验收）：

```bash
# 1. 建库（在 peixiao-m1-0610 目录下）
docker exec -i sandtable-mysql mysql -uroot -p'Sandtable@2026' < database/schema.sql

# 2. 装依赖
cd backend && pip3 install -r requirements.txt

# 3. 改 backend/.env 里 DB_PASSWORD 为你自己的密码

# 4. 启动
python3 main.py

# 5. 浏览器打开 http://127.0.0.1:8000/docs 能看到 API 文档
# 6. 浏览器打开 frontend/visit_assistant_api.html 能看到带数据的页面
```

---

## 四、交付包规范（向 peixiao-m1-0610 看齐）

每个模块席的交付包必须长这样：

```text
<名字>-m1-<月日>/
├── README.md            ← 必填：怎么建库、怎么启动、API 列表、已知问题
├── backend/
│   ├── main.py          ← FastAPI 入口（含全部路由）
│   ├── models.py        ← SQLAlchemy 表模型
│   ├── schemas.py       ← Pydantic 请求/响应
│   ├── config.py        ← 读 .env
│   ├── database.py      ← 连接池
│   ├── requirements.txt
│   └── .env             ← 数据库密码（统一 Sandtable@2026 可直接跑）
├── frontend/
│   └── <模块>_api.html  ← 对接真 API 的页面（样式抄 sandtableM1.html）
└── database/
    └── schema.sql       ← 建库建表 + 种子数据，能反复执行（DROP TABLE IF EXISTS）
```

**README 必须包含**：启动三步曲、API 表格、数据库表说明、「已知问题/没做完的」清单。
**发包方式**：压缩整个文件夹发给 Max，文件夹名带日期，如 `kaikai-m1-0624`。
**不要发**：`__pycache__/`、`node_modules/`、个人密码截图。

---

## 五、阶段划分与验收标准

> 节奏：每个阶段结束 → 交付包发 Max → Max 验收合并 → 通过才进下一阶段。
> 拜访轨已领先（S1～S3 基本完成），佳璇、开开按下表追赶。

### S0 · 环境统一（全员，目标 3 天内）

| 谁 | 做什么 |
|----|--------|
| 全员 | 装 Python + MySQL(Docker) + Cursor |
| 全员 | 跑通 `peixiao-m1-0610`（§三.3.2 六步） |
| Max | 在自己电脑复现培翛包（本手册发布时已完成）；建主工程 git 仓库 |

**✅ S0 验收标准（每人逐条核对）**
1. `python3 --version` ≥ 3.10
2. `http://127.0.0.1:8000/docs` 打开能看到「品牌沙盘 M1 · 智能拜访助手 API」
3. `frontend/visit_assistant_api.html` 打开后：品牌下拉有 5 个品牌、拜访日历有种子数据
4. 在页面上安排一条新拜访 → 刷新后还在（说明写库成功）

> 📌 **S0 验收记录（2026-06-10）**：全员通过。Max 当天下午复现培翛包（MySQL 免安装版 + 全接口冒烟 + 前端实测）；佳璇、开开当晚通过 4 条验收。另外 **S4 的第一步（培翛拜访基底入主工程）已于当天晚上提前完成**，见 §五 S4 备注——佳璇、开开 S1 交付时请直接按 `server/routers/` 结构对齐。

---

### S1 · 数据层 + 最简 API（佳璇、开开，06-11 ～ 06-12，周五晚交付）

| 谁 | 做什么 | 表 |
|----|--------|-----|
| 佳璇 | 写 `schema.sql`：在培翛的 `brands`/`brand_contacts` 基础上**新增**品牌简介与经营数据所需字段/表；后端实现 `GET /api/brands/profile/{name_key}` 返回档案数据 | `brand_profiles`（简介/潜规则）、`brand_metrics`（经营快照） |
| 开开 | 写 `schema.sql`：情报表 + 种子数据 ≥10 条；后端实现 `GET /api/intel` 列表 | `intels`（标题/正文/品牌/级别 P0P1P2/来源/时间） |
| 培翛 | 无新任务（已完成），可帮带 |
| Max | 收两份 schema.sql，检查表名/字段不与培翛的 7 张表冲突；冲突即仲裁 |

**✅ S1 验收标准**
1. `schema.sql` 在干净 MySQL 上一次执行成功，重复执行也成功
2. `http://127.0.0.1:<端口>/docs` 里 GET 接口能返回种子数据（截图）
3. 表名、字段全部 snake_case，每张表有 `created_at`/`updated_at`
4. **没有**动培翛已建的 7 张表的结构（只允许 `INSERT` 种子数据）

---

### S2 · 核心功能（佳璇、开开、培翛，06-15 ～ 06-17 核心包 + 06-22 补充包）

> ⚠️ 排期约束：周末（06-13/14）休；**06-17（三）晚 = 核心包硬交付**；06-18 是 618 大促（晚上不排、可能提前放假）；端午 06-19~21 休；**06-22（一）晚 = 补充包交付**。
>
> - **S2a 核心包（06-17 晚必交）**：佳璇＝品牌简介 Tab + 经营看板 6 指标卡；开开＝录入 + Feed 列表 + 筛选 + P0 标红
> - **S2b 补充包（06-22 晚交，原 S3 必做项）**：佳璇＝Chart.js 图表；开开＝搜索 + 周报区块；培翛＝承诺收尾
> - 移出 M1 的可选项：档案完整度计算、情报 AI 摘要（M2 再做）

| 谁 | 做什么 | 对照 demo |
|----|--------|-----------|
| 佳璇 | 品牌档案页：品牌 Tab 切换 + 「品牌简介」Tab 完整真实化（基础信息/关键人卡片可编辑/潜规则含更新人时间）+ 「经营看板」Tab 的 6 指标卡与表格 + **Chart.js 图表**（GMV 折线/渠道柱状，抄 `sandtableM1.html` 内联写法，原 S3 项） | `sandtableM1.html` → 品牌档案 |
| 开开 | 情报模块：录入表单（POST /api/intel）+ Feed 列表 + 品牌/级别筛选 + 搜索 + P0 标红 + **周报区块**（手写汇总，原 S3 项）+ brand_id 外键与培翛 brands 表对齐 | `sandtableM1.html` → 品牌情报流 |
| 培翛 | 拜访收尾（原 S3 项）：承诺状态页内可改（fulfilled/broken）；健康度表格核对基线规则（S 级 2-4 周/次） | `sandtableM1.html` → 拜访日历 |

**节奏**：06-15～06-17 主攻核心包；**06-16（二）晚中期检查**（每人发当前页面截图+已过验收条数，落后即砍范围）；**06-17（三）晚交核心包**；06-18（618）与端午不排任务；**06-22（一）晚交补充包**。

**✅ S2a 核心包验收标准（06-17 晚）**
- 佳璇：
  1. 5 个品牌可切换，数据来自数据库而非写死
  2. 关键人「编辑」能真改库（改完刷新仍在）
  3. 经营看板 6 个指标卡数值来自 `brand_metrics` 表
  4. 页面样式与 `sandtableM1.html` 品牌档案页肉眼无明显差异
- 开开：
  1. 录入一条 P0 情报 → 列表立即出现且标红
  2. 筛选「美的 + P0」结果正确
  3. 刷新页面数据仍在；`/docs` 里 POST/GET 可独立调通

**✅ S2b 补充包验收标准（06-22 晚）**
- 佳璇：图表随品牌切换刷新、无 JS 报错（F12 控制台干净）
- 开开：搜索关键词结果正确；周报区块展示本周 P0/P1 计数与列表；brand_id 与培翛 brands 表对得上
- 培翛：承诺状态修改后，`/api/commitments` 返回的状态同步变化

---

### S3 · 已并入 S2 补充包（2026-06-10 工期压缩）

> 原 S3 必做项 = S2b 补充包（见上），可选项移出 M1。本节编号保留以免引用混乱，无独立验收。

---

### S4 · 滚动合并（Max 主导，06-18 白天可预合，正式 06-22 ～ 06-24）⭐ 本项目最关键阶段

> 滚动合并：不等三模块齐，谁先交付 Max 先合并谁。**06-18（618）白天**如 Max 在岗，可提前合并 06-17 晚收到的 S2a 核心包；端午后 06-22 收 S2b 补充包，06-22～24 全力合并联调。拜访基底已于 06-10 入主工程。

Max 把三个交付包合并成一个主工程：

```text
sandtable/
├── server/                ← 合并后的唯一 FastAPI 应用
│   ├── main.py            ← 只做 app 组装 + 挂路由
│   ├── routers/
│   │   ├── visits.py      ← 来自培翛 main.py 拆分
│   │   ├── brands.py      ← 来自佳璇
│   │   └── intel.py       ← 来自开开
│   ├── models.py          ← 三份 models 合一（Max+AI 干）
│   ├── schemas.py / config.py / database.py
│   └── requirements.txt
├── web/                   ← 统一前端（以 sandtableM1.html 为壳）
│   ├── index.html         ← 侧栏四模块导航
│   └── js/api.js          ← 统一 API_BASE
└── database/
    └── schema.sql         ← 三份建表脚本合一
```

合并顺序（一次只合一个，合完测一轮）：
1. 培翛拜访（基底，先把单文件 main.py 拆成 routers 结构）
2. 佳璇品牌（处理 brands 表字段叠加）
3. 开开情报

> 📌 **S4 进度备注（2026-06-10）**：第 1 步「培翛拜访基底」已提前完成（与 S0 同日）：
> - `server/`：培翛 main.py 拆为 `routers/brands.py`（品牌/联系人/提醒，佳璇 S1 起在此扩展）+ `routers/visits.py`（拜访/记录/承诺/待办/健康度），逻辑零改动；18 个接口在 `/docs` 全部可用
> - `web/`：统一壳 `index.html`（基于 sandtableM1.html）侧栏「智能拜访助手」已跳转真模块 `visit.html`（接 API，支持 `?brand=` 带品牌跳入，带「← 工作台」返回）；品牌档案/情报流暂为静态演示，等 S2/S3 交付后替换
> - `database/schema.sql`：当前 = 培翛修复版（utf8mb4 + 可重复执行）
> - 本机启动：`cd server && python3 main.py`；前端 `http://127.0.0.1:5510/web/index.html`
> - 剩余：第 2 步合并佳璇、第 3 步合并开开，等 S1~S3 交付

**✅ S4 验收标准（整体回归）**
1. 一条命令启动 `server/`，`http://127.0.0.1:8000/docs` 同时出现拜访/品牌/情报三组 API
2. 干净 MySQL 上执行合并版 `schema.sql` 一次成功
3. 统一前端壳里四个导航都能打开，三个模块页均为真数据
4. 跨模块跳转通：品牌档案「安排拜访」→ 拜访页带上品牌；情报「查看档案」→ 档案页定位品牌
5. 走通完整业务流：录入情报 → 档案看到该品牌 → 安排拜访 → 填拜访记录 → 自动生成待办 → 改承诺状态
6. 三位模块席各自在 Max 电脑前操作自己模块 10 分钟，无报错

---

### S5 · 工作台聚合（Max，06-25 ～ 06-26，周末 06-27/28 休）

- 工作台页（只读聚合）：今日待办（来自拜访 todos）+ 近期 P0/P1 情报 + 建议联系品牌（健康度偏低的）
- ~~可选 AI：拜访纪要抽待办（接 LLM API）、情报周报摘要~~ → 工期压缩，移出 M1（M2 再做）

**✅ S5 验收标准**
1. 打开工作台一眼看到：待办数、P0/P1 数、建议拜访品牌，与各模块页数字一致
2. 待办在工作台勾完成 → 拜访模块里状态同步

### S6 · 上云 + 内部试点（Max，06-29 ～ 06-30）

> 前置：云主机资源 **06-17 前**发起申请（原 06-25，因端午+周末提前），避免卡开通流程。

- 京东云主机：MySQL + uvicorn + Nginx 静态托管前端
- 内部试点：团队 4 人在外网各自跑通完整业务流
- 外部试点用户（5～10 人）的邀请与反馈收集移到 7 月初，不阻塞 M1 交付

**✅ S6 验收标准**：外网可访问、数据不丢、并发 50 压测页面可用、团队 4 人外网跑通 S4-5 的完整业务流。

---

## 六、公共资源规则（防打架，重点读）

### 6.1 `brands` / `brand_contacts` 是公共表

- **现状即标准**：以培翛 `schema.sql` 里的定义为基准版本
- 佳璇要加字段（如成立时间、总部、定位）→ 在群里发**字段申请**，Max 批准后所有人同步：

```text
【字段申请】
申请人：佳璇
表：brands
新增：founded_year VARCHAR(16)、hq VARCHAR(64)、positioning VARCHAR(255)
原因：S2 品牌简介 Tab 需要
影响：纯新增列，不改已有列，培翛拜访模块不受影响
```

- **任何人不得**：改公共表已有列的类型/名字、删列、改种子数据里的品牌 name_key

### 6.2 五个品牌的 name_key 全队统一

`midea / joyoung / supor / bear / morphy`——前后端、种子数据、跳转参数都用这套，禁止自创拼写。

### 6.3 API 路径前缀

| 模块 | 前缀 | 例 |
|------|------|----|
| 拜访 | `/api/visits` `/api/records` `/api/commitments` `/api/todos` `/api/health` | 已定（培翛版为准） |
| 品牌 | `/api/brands/...` | 列表/详情已被培翛实现，佳璇新增的档案接口放 `/api/brands/profile/...` |
| 情报 | `/api/intel/...` | 开开自定，报 Max 备案 |

### 6.4 UI 公共样式

抄 `sandtableM1.html` 的 `:root` CSS 变量与 `.card/.tag/.level-badge/.tab-bar` 等类名。合并时 Max 会抽成 `web/css/common.css`，模块期各自内联即可。

---

## 七、模块席怎么用 AI 干活（话术模板）

### 7.1 开工模板（每个任务粘贴给 Cursor）

```text
我在开发品牌沙盘 M1 的【情报】模块，技术栈 FastAPI + SQLAlchemy + MySQL + 原生 HTML。
参考范例工程：peixiao-m1-0610/（结构、代码风格、命名全部向它看齐）。
UI 参考：sandtableM1.html 的「品牌情报流」页面。
本次任务（S2）：实现情报录入表单 + POST /api/intel 接口，存入 intels 表。
约束：
1. 不要改 brands、visits 等其它模块的表
2. 表单样式抄 sandtableM1.html 的 .visit-form 写法
3. 完成后告诉我怎么自测
```

### 7.2 报错模板

```text
我跑 python3 main.py 报错了，报错全文如下：
<粘贴完整报错>
我的环境：Mac / Python 3.12 / MySQL 8 在 Docker 里。
请告诉我原因和修复步骤，一步一步来，我不会写代码。
```

### 7.3 三条铁律（贴在显示器上）

1. **一次只让 AI 做一件小事**，做完自测再下一件；憋大招必翻车
2. **不懂的报错原文粘给 AI**，不要自己猜着改
3. **每天收工前**把整个文件夹复制一份存档（如 `kaikai-m1-0620-备份`），AI 改崩了能回滚

---

## 八、Max 的合并 SOP（给自己看）

1. 收到交付包 → 先按对方 README 原样跑通（不改任何代码），跑不通直接打回并附报错截图
2. 跑通后做该阶段验收清单，逐条截图留档到 `docs/验收记录/`
3. 合并时给 AI 的话术：

```text
把 <名字>-m1-XXXX/backend 的路由合并进 server/routers/<模块>.py：
1. 表模型追加到 server/models.py，禁止改动已有模型
2. 路由挂到主 app，保持原 API 路径不变
3. schema.sql 合并：新表追加在文件尾部，公共表只允许追加列（ALTER 语句单独成段）
4. 合并完成后 python3 main.py 必须能起，/docs 中新旧接口都在
不要重构、不要改样式、不要动其它模块文件。
```

4. 合并后回归测试（S4 验收清单 1～5），通过才 git commit，commit 信息：`merge: <模块> <阶段> from <交付包名>`
5. 任何与 demo（sandtableM1.html）不一致的业务行为 → **先记录、群里确认以哪边为准，再动手改**

---

## 九、进度与风险

### 9.1 当前进度基线（2026-06-10 晚更新）

| 轨道 | S0 | S1 (06-12) | S2a (06-17) + S2b (06-22) | 备注 |
|------|----|----|----|------|
| 拜访（培翛） | ✅ | ✅ | 🔶 仅剩承诺收尾 | 已交付 peixiao-m1-0610，且已作为基底入主工程（S4 第一步） |
| 品牌（佳璇） | ✅ 06-10 | ☐ | ☐ | 进入 S1 |
| 情报（开开） | ✅ 06-10 | ☐ | ☐ | 进入 S1 |
| Max | ✅ 复现培翛包 | — | — | S4 第一步已提前完成（06-10）：server/ + web/ 骨架就绪；06-23 起滚动合并 |

### 9.2 风险与对策

| 风险 | 对策 |
|------|------|
| ~~佳璇/开开环境装不上~~ | ~~S0 卡 2 天即结对~~（S0 已全员通过，风险解除） |
| 交付包在 Max 电脑跑不通 | 打回自查，README 启动步骤必须可复制粘贴执行 |
| 公共表被改坏 | 公共表只增不改；Max 合并前 diff schema.sql |
| **06-16 中期检查落后**（压缩工期主风险） | 第一刀：S2b 补充项再砍（图表/周报移 M2）；第二刀：佳璇经营看板只留品牌简介 Tab、情报只留录入+筛选；没做完的区块挂「建设中」照常进 S4 |
| 06-17 核心包交不出 | 当晚有什么交什么，Max 以现状合并；缺的功能列入 S2b 于 06-22 补 |
| 提前放假/618 占用 | 06-17 晚为最后硬时点，之后到 06-21 一律不依赖任何人在岗；Max 06-18 白天的预合并为可选加分项 |
| 端午后状态回不来 | 06-22 当晚必须交补充包（哪怕不完整），Max 以现状滚动合并，缺的合并后继续补 |
| S6 上云卡壳（京东云开通/备案等外部依赖） | **06-17 前**发起云主机申请；若 06-30 前外网不可用，降级为内网演示验收，上云顺延至 7 月第一周 |
| AI 改崩找不回 | 每日收工备份文件夹（铁律 3） |

---

## 附录 A · 培翛包 API 一览（即拜访模块 API 标准）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/brands` | 品牌下拉（按等级排序） |
| GET | `/api/brands/detail` | 品牌列表含温度/完整度 |
| GET | `/api/brands/{name_key}` | 品牌详情 |
| GET | `/api/brands/{brand_id}/contacts` | 联系人 |
| GET | `/api/brands/{name_key}/reminder` | 拜访前提醒（未兑现承诺/过期联系人） |
| POST/GET | `/api/visits` | 安排/列表（支持 status、brand_id、month 筛选） |
| GET/PUT/DELETE | `/api/visits/{id}` | 详情/改状态/删 |
| POST/GET | `/api/records` | 拜访记录（POST 自动生成待办）|
| GET/PUT | `/api/commitments` | 承诺列表/改状态 |
| GET/PUT | `/api/todos` | 待办列表/改状态 |
| GET | `/api/health` | 90 天拜访频率健康度 |

## 附录 B · 数据库表归属

| 表 | Owner | 其他人权限 |
|----|-------|-----------|
| brands、brand_contacts | 公共（基准=培翛版，业务 owner=佳璇） | 只读 + 字段申请 |
| visits、visit_attendees、visit_records、commitments、todos | 培翛 | 只读 |
| brand_profiles、brand_metrics（S1 新建） | 佳璇 | 只读 |
| intels（S1 新建） | 开开 | 只读 |

---

*维护人：Max。改版时更新版本号并群里通知。*
